# Deployed frontend (e.g. Vercel): set this in the host’s Environment Variables UI, then rebuild.
# Your Express API’s public origin — no trailing slash.
# Example: https://ccs-api.onrender.com
VITE_API_BASE=
